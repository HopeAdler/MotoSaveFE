import { Card, Typography } from "antd";

export default function DistanceRateList() {
  return (
    <Card className="p-6 shadow-lg">
      <Typography className="text-2xl font-bold mb-4">Distance Rate</Typography>
      <Typography className="text-lg">Manage distance rate calculations.</Typography>
    </Card>)
}
